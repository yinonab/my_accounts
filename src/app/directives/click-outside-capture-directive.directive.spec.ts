import { ClickOutsideCaptureDirectiveDirective } from './click-outside-capture-directive.directive';

describe('ClickOutsideCaptureDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOutsideCaptureDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
