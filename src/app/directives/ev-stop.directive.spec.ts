import { EvStopDirective } from './ev-stop.directive';

describe('EvStopDirective', () => {
  it('should create an instance', () => {
    const directive = new EvStopDirective();
    expect(directive).toBeTruthy();
  });
});
